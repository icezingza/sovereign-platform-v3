# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`sovereign-platform-v3`) is currently empty. This CLAUDE.md will be updated once the project stack, structure, and conventions are established.

When the codebase is populated, update this file with:

- **Build/lint/test commands** — including how to run a single test file or suite
- **Architecture overview** — the high-level structure that isn't obvious from browsing files (e.g. how services communicate, data flow between layers, key abstractions)
- **Non-obvious conventions** — naming rules, branching strategy, code generation steps, environment setup quirks
- **External dependencies or services** — databases, queues, third-party APIs the code integrates with
